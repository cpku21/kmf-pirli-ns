/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'minifudbalsrbije.rs',
      'i.imgur.com',
      'res.cloudinary.com'
    ],
  },
};

export default nextConfig;
